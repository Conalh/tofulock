# Build output
/tofulock
/tofulock.exe
/dist/

# Go
*.test
*.out
coverage.*

# Attestation signing keys — never commit
*.key
*.pub
# Signed DSSE envelopes (regenerable; unsigned tofulock.attestation.json is committed as an example)
*.dsse.json

# Local adoption-feedback notes (not part of the public repo)
/docs/feedback/

# Editor / OS
.DS_Store
.idea/
.vscode/
