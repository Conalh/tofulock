# Build output
/tofulock
/tofulock.exe
/dist/

# Go
*.test
*.out
coverage.*

# Attestation signing keys — never commit
*.key
*.pub
# Signed DSSE envelopes (regenerable; unsigned tofulock.attestation.json is committed as an example)
*.dsse.json

# Local diligence / business notes
/docs/diligence/

# Editor / OS
.DS_Store
.idea/
.vscode/
